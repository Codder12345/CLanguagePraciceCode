#include<stdio.h>
#include<string.h>
int main()
{
	 char ch[5];
      printf("\n Enter string from keborad:");
        gets(ch);
       strupr(ch);
      printf("%s:",ch);
        strlength(ch);
	 return 0;
}
